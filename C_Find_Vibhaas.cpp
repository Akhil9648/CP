#include <bits/stdc++.h>
using namespace std;
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int lowx=0,highx=1e9;
    int lowy=0,highy=1e9;
    while(lowx<=highx && lowy<=highy){
        int midx=(lowx+highx)/2;
        int midy=(lowy+highy)/2;
        string s=(cout<<?midx midy);
        if(s=="HERE") cout<<!midx midy<<endl;
        
    }
}
